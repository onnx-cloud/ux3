name: News load
steps:
  - type: event
    machine: newsFSM
    name: LOADED
    payload:
      - id: 1
        title: "Headline"
        body: "Some body"
  - type: fsmState
    machine: newsFSM
    equals: loaded
