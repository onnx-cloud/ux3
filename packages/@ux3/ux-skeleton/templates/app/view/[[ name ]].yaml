name: [[ name ]]
layout: default
initial: index
context:
  loading: false
  error: null
states:
  index:
    template: widget/[[ name ]]/index.html
    on:
      SUBMIT:
        actions: [handle[[ Name ]]]
      '*': {}