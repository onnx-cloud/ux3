{
  "name": "@ux3/plugin-stripe",
  "version": "0.1.0",
  "description": "Stripe payment integration for UX3 — PCI-compliant tokenization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "sideEffects": false,
  "peerDependencies": {
    "@stripe/stripe-js": "^2.0.0"
  },
  "peerDependenciesMeta": {
    "@stripe/stripe-js": { "optional": true }
  }
}
