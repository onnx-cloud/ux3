import type { FSMContext, FSMEvent } from '@ux3/core';

export async function handle[[ Name ]]Submit(
  ctx: FSMContext,
  event: FSMEvent
): Promise<void> {
  // TODO: implement [[ name ]] submit logic
  // Throw to signal ERROR, return normally to signal SUCCESS.
}
