initial: idle
states:
  idle:
    template: widget/[[ name ]]/idle.html
    on:
      SUBMIT: submitting
  submitting:
    template: widget/[[ name ]]/submitting.html
    invoke:
      src: handle[[ Name ]]Submit
    on:
      SUCCESS: done
      ERROR: idle
  done: widget/[[ name ]]/done.html
