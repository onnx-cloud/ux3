/**
 * Widget System - Core Abstractions
 * 
 * Widgets are the core abstraction for UI composition:
 * - Bind to state machines and templates
 * - Support nesting and composition
 * 
 * Widgets are reusable UI that:
 * 1. Render HTML (with interpolations, bindings)
 * 2. Can bind to FSM state machines
 * 3. Support composition (nesting other widgets)
 * 4. Are independent of view components
 */
